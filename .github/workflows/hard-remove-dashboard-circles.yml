name: Hard remove dashboard circles
on:
  push:
    paths:
      - '.github/workflows/hard-remove-dashboard-circles.yml'
permissions:
  contents: write
jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "completed"
